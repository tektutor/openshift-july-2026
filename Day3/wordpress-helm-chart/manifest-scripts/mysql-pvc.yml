apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mysql-pvc-{{.Values.your_name}}
spec:
  selector:
    matchLabels:
      name: {{.Values.your_name}}
  accessModes:
  - ReadWriteOnce
  volumeMode: Filesystem
  resources:
    requests:
      storage: {{.Values.msql_pv_size}} 
