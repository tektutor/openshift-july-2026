# Shared ServiceAccount. The upstream mariadb and wordpress images run as a
# fixed UID (mariadb switches to the mysql user; wordpress binds port 80), so
# deploy.sh grants this SA the anyuid SCC. See README for a non-root option.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: wp-mariadb-sa
